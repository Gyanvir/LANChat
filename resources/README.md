# Resources Folder

The application styling uses a Telegram-inspired dark stylesheet (QSS) injected programmatically inside [MainWindow.cpp](file:///e:/SDE_intern_Task/LANChat/src/MainWindow.cpp). 

Any additional graphical assets, icons, or custom stylesheets in the future should be placed in this directory and registered via a `.qrc` Qt Resource file.
